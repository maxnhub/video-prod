.banner {
  &__text {
    font-size: 48px;
    font-weight: bold;
    color: #fff;
    &-orange {
      color: $second-color;
    }
    @include phones {
      font-size: 39px;
      text-align: center;
    }
  }
}